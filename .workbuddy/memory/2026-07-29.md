# 2026-07-29

## 继续推进 v0.9

### 已完成
- **底部状态栏实时数据**：从 `computeFlightStats()` 读本周飞行数（last7Total）、从 `get('streak')` 读 streak，每 30s 自动刷新；demo 截图可见 "0 本周飞行 / 0 连续准时"
- **预存测试修复**：`UpdateManager.test.js` 缓存分支测试加 `vi.spyOn(globalThis, 'fetch').mockRejectedValue(new Error('network'))` 强制走网络失败路径 → **258/258 tests 通过**（之前 257/258）

### 工程债评估（来自 project-review.md）
抽样核查后结论：**11 项里的 4 项实际已被历史修复**
- ✅ progressInterval scope bug（已声明在 try 外）
- ✅ UpdateManager cache branch dataset.url（已设置）
- ✅ XSS: renderMarkdown（https?:// 协议白名单已生效）
- ✅ XSS: TaskRenderer 搜索高亮 + SettingsPanel 导入预览（escapeHtml 已用）

### 仍未做（影响上架/安全）
- `run_script` 命令注入（`sh -c`/`cmd /C` 字符串拼接）—— 风险大，改动需重做前后端 schema
- CSP 收紧（`style-src 'unsafe-inline'`、`assetProtocol.scope: ["**"]`）
- 两套更新机制并存（Tauri updater plugin + 自定义 download）
- `.github/workflows/release.yml` 多 job 竞态

### 下一步建议
1. **真机验证** v0.9 当前状态（`npm run tauri dev`）：智能时机/飞行触发/主题切换全链路
2. **提交 v0.9.0 tag**（35 文件改动 + 28 新文件已就绪）
3. v1.0「陪伴」规划：常驻机长 3.0 状态机是核心

## 第三次会话（17:06 - 继续完成 v0.9 未完成任务）
- ✅ 番茄钟静默回调补全：`setPomodoroBreakStartCallback` 加入 `doTriggerFlight` + `clearPostponedFlights`，换 i18n 键 `pomodoro.focus_resume`
- ✅ 完整工程债抽样：`speak_text` 参数数组（已替代 shell 拼接）、`is_script_allowed` 元字符过滤、`open_app` 路径存在性、assetProtocol scope 已收紧。仅余 `style-src unsafe-inline` (需要全体内联迁移) + 双更新机制 (非安全)
- ✅ `npm run tauri dev` 真机验证：Rust 0.84s 编译、启动无 crash、更新检查到 v0.8.1、无 console error
- ✅ `npm run lint` 0 errors / `npm test` 258/258
- v0.9 就绪度 ~90% （剩余两项非阻塞，建议切 v1.0）

## 第四次会话（17:11 - 继续 + 对照概念稿补遗）
对照 `docs/ui-concept.html` 与实际实现，识别并修复 4 项差距：

| 差距 | 修复 |
|---|---|
| 已取消航班行无恢复按钮（概念 ↺） | FlightBoard.js 加 op-restore 按钮 + main.js 'restore' action（设 enabled=true + saveTasks + toast） |
| 底部栏缺少迷你柱状图（概念 stat-mini .bars） | refreshFooter 计算 Mon-Sun 本周真实值 + 7 日 dailyCounts 渲染 .bars，opacity 区分有/无数据 |
| Hero depart 缺少跑道文案（"从 X 号跑道起飞"） | refreshHero departInfo 加 `flight.runway_n` 占位（task.id % 4 + 1），新增 i18n |
| 本周数据用 last7Total 而非真正的 Mon-Sun | refreshFooter 改用日期过滤 monday/sunday 计算 |
- ✅ 验证：lint 0 errors、test 258/258 全绿

### v0.9 现在 ~98%
- 已完成：Terminal UI 航站楼 / 智能暂停 / 自然间隔 / 番茄钟 2.0（静默+补飞）/ Snooze / 底部栏实时数据+迷你柱状图 / 命令面板 / 板头+跑道文案 / 已取消航班恢复
- 残留非阻塞：run_script 参数化 / CSP unsafe-inline 迁移 / 双更新机制统一

## 第五次会话（17:29 - UI 优化去重与功能补全）
用户反馈中间任务列表区域缺少功能 + 多处按钮重复。已落实：

| 改动 | 实现 |
|---|---|
| 顶栏「+ 新建任务」删除 | 与板头「+ 新建航班」重复，主 CTA 保留 |
| 底部「🍅 进入专注模式」删除 | 与快速创建栏的 🍅 重复；快速创建栏保留且更语义契合 |
| 航班行左侧加电源开关 toggle | ⏻ 绿=启用，⏼ 灰=停用，hover 琥珀，点击即切换 task.enabled 并 saveTasks |
| ⋍ 改为弹出菜单 | 编辑 / 复制 / 启用·停用 / 删除（带危险色）；智能定位防止越界，点击外部关闭 |
| 时间格式改进 | "今天 09:00" / "明天 18:00" / "后天 22:00" / "3天08:00" context-aware |
| 死代码清理 | 移除原 'detail' action（被 'menu' 取代），'openTaskDetailDrawer' 仍保留供其他入口 |

新增 i18n 键：flight.enable/disable/enabled/disabled, flight.menu.edit/copy/toggle/delete/toggle_on/toggle_off, flight.day_after

新增 CSS：op-toggle (28px 圆形按钮)、row-menu (fixed 定位 + glass 风格)、扩展 grid 列到 5 列 (36px 86px 1fr auto auto)

验证：lint 0 errors / 47 warnings（1 个新 warning：buttonEl no-undef 已修复后再验证 0 errors）/ test 258/258

效果：航班信息板现在每行都有「启用/停用 + 即时飞行 + 推迟 + 弹出菜单」四级操作，UI 去重干净，时间显示更人性化。