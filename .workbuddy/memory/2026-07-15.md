# 2026-07-15

## 下期开发计划（v0.9 → v1.0）
为 guguFly 制定下期开发计划，已写入 `docs/next-phase-plan.md`。

依据 `src/` 代码探查发现的关键缺口：
- 旗舰功能零测试：QuickCreateParser / PomodoroTimer / HolidayPresets
- 主窗口仅 1 个 480px 断点，无响应式（style.css:3226）
- i18n 硬编码中文兜底：FlightTrigger.js:12、UpdateManager.js:209/245、utils.js:6
- 统计面板全量重算 + HistoryPanel O(n²) 聚合
- 模块初始化顺序脆弱（before init 守卫）

规划三阶段：
- v0.9.0 打磨（P0）：补测试、主窗响应式+常驻迷你条、i18n收口、初始化解耦
- v0.9.x 智能预览（P1）：数据洞察升级、情景模式、专注整合、本地重复建议
- v1.0 Pro 基石（P2）：飞行编排可视化编辑器、WebDAV云备份、主题/同步

用户未指定具体落哪一项，先交付完整规划文档供决策。

## v0.9.0 打磨地基 详细设计（docs/v090-design.md）
用户要求细化阶段一设计。补充探查关键约束：
- 主窗口 tauri.conf.json 锁死 minWidth/maxWidth=620（响应式前提：先放开）
- 迷你窗已实现（MiniWindow.js 独立 WebviewWindow），但 public/mini.html/mini.js 不走 Vite（双份维护）→ 设计改为统一进 Vite 构建
- "before init" 守卫仅 3 处：FlightOrchestrator.js:395 / QuickCreateBar.js:5 / ModalController.js:5
- 测试为 vitest 纯函数可测；QuickCreateParser 用 initI18n 即可，PomodoroTimer 需先确认是否纯 JS
设计按 功能/界面/操作 三维度展开，含文件函数级实现要点与验收清单。
